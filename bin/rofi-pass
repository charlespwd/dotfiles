#!/bin/bash

menu="rofi -dmenu -i -matching fuzzy -levenshtein-sort -p lpass:"
passinfo=$(lpass ls --format '%an - %au - %ai' | $menu)
passid=$(echo "$passinfo" | awk -F' - ' '{ print $3 }')
lpass show -cp $(passid)
